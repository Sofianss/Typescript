export default class Joueur {
    private _nom: string;
    private _score: number;

    constructor(nom: string, score: number){

        this._nom = nom;
        this._score = score;

    }

    public jouer(): void {
        return
    }
}